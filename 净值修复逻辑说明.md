# 净值修复逻辑说明

更新时间：2026-04-22 CST

本文档用于整理当前 [fix_nav_confusion.py](/Users/hkk/Documents/净值监控/fix_nav_confusion.py) 的真实修复逻辑，并同步最近一次全量运行后的上下文状态。

## 1. 目标

当前任务是修复基金净值数据中的口径混淆和局部异常，重点处理：

- `UnitNAV`：单位净值
- `AccuNAV`：累计净值
- `AccuDividends`：累计分红
- `Dividends`：当期分红

基础关系：

- `AccuNAV = UnitNAV + AccuDividends`
- `Dividends = diff(AccuDividends)`

当前成功定义：

- 修复后无负分红
- 修复后无收益率异常
- 修复后无分红过频

任一异常仍存在，则该基金视为失败。

## 2. 当前默认参数

当前参数以 [thresholds.json](/Users/hkk/Documents/净值监控/thresholds.json) 为准：

- `tol = 0.003`
- `dividend_window_days = 20`
- `nav_change_threshold = 0.20`
- `max_outlier_span = 10`
- `context_run_min_len = 2`
- `context_neighbor_max_gap = 12`
- `stable_dividend_run_days = 5`
- `dividend_spike_max_span = 5`

说明：

- `tol` 用于判断累计分红变化是否可视为 0
- `nav_change_threshold` 当前按 20% 判断收益率/跳变异常
- `max_outlier_span` 当前允许最多 10 条记录组成的异常块被整体剔除

## 3. 异常定义

### 3.1 负分红

- 先按 `AccuDividends = AccuNAV - UnitNAV` 计算累计分红
- 再按相邻有效日期计算 `Dividends = diff(AccuDividends)`
- 若 `Dividends < -tol`，判为负分红异常

此外，`AccuDividends < -tol` 本身也视为非法状态，当前已被提高为强约束，候选路径不能再轻易选中。

### 3.2 收益率异常

当前实现口径：

- 单位净值收益率：
  `(当期分红 + 当期 UnitNAV) / 上期 UnitNAV - 1`
- 累计净值收益率：
  `AccuNAV_t / AccuNAV_{t-1} - 1`

任一绝对值超过 `nav_change_threshold`，记为 `return_anomaly_flag`。

### 3.3 分红过频

- 若某次有效分红为正
- 且距离上一次有效分红的自然日天数 `<= dividend_window_days`
- 则记为 `dividend_frequency_flag`

### 3.4 孤点 / 异常块

当前异常块有两类：

- `isolated_jump_block`
- `dividend_spike_revert_block`

共同原则：

- 是连续短区间
- 去掉后，前后主体趋势重新连起来
- 被剔除后不参与最终验收

## 4. 数据预处理

### 4.1 输入形式

脚本支持：

- 单个 Excel 文件
- 按基金拆分后的目录 [按基金拆分](/Users/hkk/Documents/净值监控/按基金拆分)

当前默认输入是按基金拆分目录。

### 4.2 文件过滤

目录输入时，会忽略 Excel 临时文件：

- `.~*.xlsx`
- `~$*.xlsx`

### 4.3 同日去重

对同一只基金同一交易日的重复记录：

- 按原始顺序保留第一条
- 保留 `source_row_count`
- 保留最早原始行号 `_row_id`

### 4.4 `AccuNAV < UnitNAV` 预处理

当前已新增一条预处理规则：

- 若 `AccuNAV < UnitNAV`
- 先交换两列
- 再进入后续修复流程

输出中会记录：

- `preprocess_swapped`
- `preprocess_reason`

## 5. 当前修复框架

当前修复已经不是单一规则，而是多层框架：

1. 读取数据并做数值清洗
2. 按 `FundID + TradingDay` 去重
3. 预处理 `AccuNAV < UnitNAV` 的记录
4. 计算 `raw_accu_div`
5. 按累计分红运行段生成上下文推断
6. 生成每行候选修复方案
7. 动态规划选择全局最低成本路径
8. 标记异常块并剔除
9. 对剔除后剩余数据再次重跑路径修复
10. 重算三类异常标记
11. 输出修复结果与汇总

## 6. 候选修复模式

当前脚本可能生成的主要候选包括：

- `original`
- `swap`
- `prev_div_*`
- `context_div_*`
- `surrounding_div_match_*`
- `future_dividend_backfill_*`
- `stable_dividend_revert_*`
- `shared_zero_after_positive_*`
- `repeated_nonzero_bridge_*`

围绕目标累计分红 `target_div`，主要重建方向仍然是：

- `unit_as_unit`
- `accu_as_accu`
- `accu_as_unit`
- `unit_as_accu`

当前高频且相对稳定的仍是：

- `unit_as_unit`
- `accu_as_accu`

## 7. 上下文累计分红推断逻辑

### 7.1 `surrounding_div_match`

适用条件：

- 当前段前后能找到近邻有效运行段
- 前后累计分红几乎一致
- 当前段与前后不一致

用途：

- 修正中间一小段累计分红错位
- 修正中间一小段 `UnitNAV / AccuNAV` 共享值误填

### 7.2 `future_dividend_backfill`

适用条件：

- 未来累计分红高于当前累计分红
- 原始收益率异常
- 但把未来分红提前回填后，收益率异常消失

用途：

- 处理“分红应更早反映”的情况

### 7.3 `stable_dividend_revert`

适用条件：

- 当前短段前后是稳定平台
- 前后平台值一致
- 当前短段偏离平台

当前还加了一层更强约束：

- 如果当前正分红短段本质上像孤立尖刺
- 且后面同样的正分红平台不会继续重复
- 会更强地回退为 `0`

### 7.4 `shared_zero_after_positive`

适用条件：

- 一小段 `UnitNAV == AccuNAV`
- 当前累计分红约等于 `0`
- 紧接在正分红平台后面

用途：

- 处理“累计分红突然从正值掉成 0，说明两列共用了同一值”的短段

### 7.5 `repeated_nonzero_bridge`

适用条件：

- 一段共享值零平台
- 前后都存在重复出现的同一个非零累计分红平台

用途：

- 处理像 [943386.xlsx](/Users/hkk/Documents/净值监控/按基金拆分/943386.xlsx) 这种“长期共享值零平台”和“重复非零平台”交替出现的情况

当前处理策略：

- 可作为高优先级候选
- 在一部分高置信度情况下会被强制
- 不再对所有桥接段一律强制，避免副作用过大

## 8. 共享值混用修复逻辑

共享值的典型特征：

- `UnitNAV == AccuNAV`
- `AccuDividends` 突然从正值掉到 `0`
- 或者长期 `0` 与非零平台反复打架

当前共享值判断原则：

- 若共享值更接近正确 `AccuNAV` 链，则用 `accu_as_accu`
- 若共享值更接近正确 `UnitNAV` 链，则用 `unit_as_unit`

实现细节：

- 先给共享段算 `context_dividend`
- 再打 `context_mode_hint`
- 若方向足够明确，则 `context_mode_locked = True`
- 某些高置信度段会 `force_context_repair = True`

新增的细节：

- 长零共享段不再一律用最近邻拍方向
- 会参考前后同值非零平台和共享值平台切分后的连续性
- 对明显不应作为锚点的孤立正分红尖刺，会从锚点候选中排除

## 9. 孤点剔除逻辑

孤点处理放在第一次修复后，再进入一次重跑。

流程：

1. 先跑一次全局修复
2. 识别 `isolated_jump_block` 和 `dividend_spike_revert_block`
3. 将这些记录标记为：
   - `exclude_as_outlier = True`
   - `exclusion_reason = ...`
4. 对未剔除的主体序列重新跑修复
5. 重新计算最终异常标记

当前已验证的典型样本：

- [393514.xlsx](/Users/hkk/Documents/净值监控/按基金拆分/393514.xlsx)
- [893822.xlsx](/Users/hkk/Documents/净值监控/按基金拆分/893822.xlsx)

## 10. 动态规划选路原则

动态规划的成本设计当前大致是：

- 强烈惩罚负累计分红和负分红
- 明显惩罚收益率异常
- 惩罚分红过频
- 惩罚频繁切换修复模式
- 惩罚偏离原始值过大
- 非 `original` 候选有轻微基础成本

当前又加了一条重要约束：

- `AccuDividends < -tol` 的候选现在会被直接判成强失败状态
- 不再允许靠“高罚分但仍可选”混进最终路径

## 11. 当前全量状态

最新全量运行命令：

```bash
/Users/hkk/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 fix_nav_confusion.py --input '按基金拆分' --output-dir 'outputs' --config 'thresholds.json'
```

最新全量结果：

- `total_funds = 52`
- `success_funds = 41`
- `failed_funds = 11`
- `remaining_neg_dividend_rows = 5`
- `remaining_return_anomaly_rows = 19`
- `remaining_dividend_frequency_rows = 9`

当前仍失败基金：

- `942574`
- `394162`
- `942775`
- `943386`
- `1648828`
- `943458`
- `404802`
- `494282`
- `944765`
- `944791`
- `1550846`

## 12. 当前重点案例状态

### 12.1 `393514`

- 已成功
- 主要依靠异常块剔除修复
- `2025-04-30` 到 `2025-05-04` 的跳点可被识别并剔除

### 12.2 `893822`

- 已成功
- `2025-11-14` 到 `2025-11-20` 的脏数据在 `max_outlier_span = 10` 下可被整体剔除

### 12.3 `404802`

- 仍失败
- 更像两条累计分红链竞争
- 后续更适合做“从最新尾部往前的链选择”，而不是继续大量小段修补

### 12.4 `943386`

- 已从“负分红 + 收益率异常并存”压缩到只剩 1 条负分红
- 当前唯一残留点在 `2022-01-28`
- 这只基金说明：
  - 共享值零段桥接是有效的
  - 但最前面的 regime 切换点仍需要更明确的链选择或硬规则

### 12.5 `943458`

- `2025-01-14` 的共享值错误已经能识别并修复
- 当前失败不在“相等值没识别”，而在后续连续性仍有 3 条收益率异常

## 13. 输出文件

当前标准输出：

- [具体异常净值_修正结果.xlsx](/Users/hkk/Documents/净值监控/outputs/具体异常净值_修正结果.xlsx)
- [修复汇总.csv](/Users/hkk/Documents/净值监控/outputs/修复汇总.csv)
- [修复失败明细.csv](/Users/hkk/Documents/净值监控/outputs/修复失败明细.csv)

其中 `具体异常净值_修正结果.xlsx` 包含：

- `compare_raw_fixed`
- `repaired_data`
- `repair_summary`
- `issue_details`
