# 关键案例说明

这份文档记录当前项目里已经反复验证过的典型异常模式，以及它们对应的修复思路。它的目标不是替代算法文档，而是帮助后续维护时快速判断：

- 某条规则是在保护什么
- 哪类基金已经验证过
- 新规则会不会把旧案例修坏

## Case 1: `393514`

文件：
- [393514.xlsx](/Users/hkk/Documents/净值监控/按基金拆分/393514.xlsx)

典型现象：
- `2025-04-30` 附近出现明显跳点
- 这类点可能连续出现几天
- 去掉这一小段后，前后净值路径重新接近

修复思路：
- 不把它当成正常分红
- 把它识别为 `isolated_jump_block`
- 显式标记 `exclude_as_outlier = True`

为什么重要：
- 这是“孤点块”而不是“字段混淆”
- 如果把这种数据硬修成另一种口径，通常会把原本没问题的序列修坏

## Case 2: `893822`

文件：
- [893822.xlsx](/Users/hkk/Documents/净值监控/按基金拆分/893822.xlsx)

典型现象：
- `2025-11-14` 到 `2025-11-20` 有一段连续脏数据
- 异常块长度达到 `6`

修复思路：
- 识别为 `dividend_spike_revert_block`
- 扩大 `max_outlier_span` 到 `10`，允许更长的异常块整体剔除

为什么重要：
- 这个案例说明，异常块不一定只有 1 到 3 天
- 长度阈值过小会导致本应整体剔除的脏块漏掉

## Case 3: `942574`

文件：
- [942574.xlsx](/Users/hkk/Documents/净值监控/按基金拆分/942574.xlsx)

典型现象：
- 有些日期 `AccuNAV < UnitNAV`
- 有些日期 `UnitNAV == AccuNAV`，并且累计分红从正值突然掉到 `0`

修复思路：
- 预处理阶段先执行 `AccuNAV < UnitNAV` 交换
- 对“共享值归零”这类情况，使用 `shared_zero_after_positive`

为什么重要：
- 这类错误不是单一模式
- 同一只基金里可能同时存在“字段反了”和“共享值误填”

## Case 4: `943386`

文件：
- [943386.xlsx](/Users/hkk/Documents/净值监控/按基金拆分/943386.xlsx)

典型现象：
- 前面一大段时间里，累计分红真实平台曾经是 `0.55`
- 中间大部分时间却反复回到 `0`
- 同时夹杂短促的 `0.55` 脉冲

修复思路：
- 不是单日修补，而是看作“累计分红 regime 混用”
- 当前用到的关键规则是 `repeated_nonzero_bridge`
- 但这只基金仍然提示：只靠局部 bridge 规则还不够，可能最终需要更强的“链选择 / regime 选择”

为什么重要：
- 这是当前最能代表“长期混用而不是偶发错值”的案例
- 它提醒我们：有些基金的正确修法，不是点修，而是选整条链

## Case 5: `943458`

文件：
- [943458.xlsx](/Users/hkk/Documents/净值监控/按基金拆分/943458.xlsx)

典型现象：
- `2025-01-14` 原始数据里 `UnitNAV == AccuNAV`
- 次日数值恢复成非共享状态，但整段连续性仍然可能异常

修复思路：
- 共享值当天要修
- 但不能只看当天，要关注共享值点前后短窗口的连续性

为什么重要：
- 它说明“发现共享值错误”不等于“整段已经修好”
- 共享值点往往只是小窗口异常的起点

## Case 6: `404802`

文件：
- [404802.xlsx](/Users/hkk/Documents/净值监控/按基金拆分/404802.xlsx)

典型现象：
- 累计分红在多个平台之间反复跳
- 更像是两套净值链交替写入，而不是一条链上的零星误填

修复思路：
- 当前判断是：这类基金更适合做“链选择”
- 即从最新稳定尾部往前倒推，保留与最新链条连续自洽的一套，剔除另一套

为什么重要：
- 它代表的是“两个系列在打架”
- 再继续堆局部规则，通常不如先做主链选择来得干净

## 使用建议

当新增或调整规则时，建议至少回看这些案例：

- `393514`：确认没有把孤点误修成正常点
- `893822`：确认较长异常块仍能整体剔除
- `942574`：确认预处理交换和共享值归零仍生效
- `943386`：确认长期混用场景没有被过度局部化
- `943458`：确认共享值修复后短窗口连续性没有变差
- `404802`：确认没有把双链竞争误当成单链局部噪声
